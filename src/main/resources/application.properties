spring.application.name=WorkspaceService

# MongoDB Configuration
spring.data.mongodb.host=montunoblenumbat2404
spring.data.mongodb.port=27017
spring.data.mongodb.database=CapitalMarkets

# SSL: run generate-keystore.bat (or .sh) once, then start with --spring.profiles.active=ssl
# App will listen on https://localhost:8443

# SSL profile: run with --spring.profiles.active=ssl after generating keystore
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=changeit
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=workspaceservice
server.port=8443
